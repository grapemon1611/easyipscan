<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Android 15+ (SDK 35) theme with enforced edge-to-edge support -->
    <style name="Theme.EasyIPScanner" parent="android:Theme.Material.Light.NoActionBar">
        <!-- Enable edge-to-edge display -->
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <!-- Ensure content draws behind system bars -->
        <item name="android:windowTranslucentStatus">false</item>
        <item name="android:windowTranslucentNavigation">false</item>
        <!-- Android 15+ enforced edge-to-edge: explicitly don't opt out -->
        <item name="android:windowOptOutEdgeToEdgeEnforcement">false</item>
    </style>
</resources>
